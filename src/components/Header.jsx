import React from "react";
import "../App.css"; // Use same CSS for simplicity

const Header = () => {
  return (
    <header className="header">
      <h1 className="header-title">📝 QuickNote</h1>
    </header>
  );
};

export default Header;
